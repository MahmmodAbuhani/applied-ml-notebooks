# Figure Provenance

These PNGs are compact reviewer previews generated by the corresponding source notebooks. They are not independent reports, live dashboards, or production-monitoring outputs.

| Figure | Source notebook and data | What it shows | Boundary |
| --- | --- | --- | --- |
| `bank_marketing_precision_recall.png` | Bank Marketing notebook; pinned UCI archive | Early pooled OOF and late precision-recall curves | Late ranking is weak; `duration` is excluded |
| `bank_marketing_contact_budget_curve.png` | Bank Marketing notebook; pinned UCI archive | Fixed contact-budget lift sensitivity | The top-1% concentration is exploratory, not policy evidence |
| `bank_marketing_cumulative_gains.png` | Bank Marketing notebook; pinned UCI archive | Responder capture as contact share expands | Source-order stress test, not timestamped deployment validation |
| `bank_marketing_calibration.png` | Bank Marketing notebook; pinned UCI archive | Late score-band calibration diagnostic | Scores are not calibrated probabilities |
| `bank_marketing_permutation_importance.png` | Bank Marketing notebook; pinned UCI archive | Late-segment permutation importance | Descriptive after final fit, not causal evidence |
| `palmer_penguins_permutation_importance.png` | Penguins notebook; pinned public CSV | Holdout permutation importance | Educational local demo, not a wildlife field tool |
| `decision_tree_feature_importances.png` | Decision Tree notebook; packaged Breast Cancer Wisconsin data | Tree-based feature importance | Descriptive, not causal or clinical guidance |
| `digits_knn_validation_curve.png` | kNN notebook; packaged Digits data | Train-only validation scores across `k` | Benchmark selection evidence only |
| `kmeans_wine_clusters.png` | k-means notebook; packaged Wine data | Selected `k=3` in a two-dimensional PCA view | PCA is a projection; labels are used only post hoc |
| `regression_predicted_vs_actual.png` | Regression notebook; packaged Diabetes data | Holdout predictions against observed target values | Educational benchmark, not clinical prediction evidence |
| `iris_random_forest_importances.png` | Tree ensemble notebook; packaged Iris data | Random-forest feature importance | Compact teaching example, not general biological inference |

Exact public-data identities and source links are recorded in [`docs/DATA_SOURCES.md`](../../docs/DATA_SOURCES.md). The durable Bank HTML snapshot has a separate machine-readable manifest under [`reports/evidence/`](../../reports/evidence/).
